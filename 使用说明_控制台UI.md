# Amazing Hand 控制台 · 小白使用说明（Linux）

一句话：**这只手怎么设ID、怎么标定、怎么跑Demo、怎么用摄像头手势控制，全在这一个窗口里点按钮完成。**

---

## 一、第一次使用（只做一次）

1. 解压这个压缩包，得到 `AmazingHand-小白包/` 文件夹。
2. 打开这个文件夹，找到 **`安装.sh`**：
   - 右键 → 在终端中运行；或者打开终端，`cd` 进这个文件夹后运行 `bash 安装.sh`
   - 中途会让你输**开机密码**（装系统组件用），照输即可。
   - 网络用的是国内清华镜像，正常几分钟装完。
3. 装完后，如果提示「加入 dialout 组需要重新登录」，就**注销再登录一次**（或重启），否则连不上串口。

> 只要手势控制想跑「真手」，第一次装时用 `bash 安装.sh --with-rust`（多装一个 Rust）。只玩控制/标定/仿真不用加。

---

## 二、打开控制台

- 双击 **`打开控制台.sh`**（弹框选「运行」）；
- 或终端里 `bash 打开控制台.sh`。

窗口标题是「**Amazing Hand 控制台**」。

**顶部一排**：
- `串口` 下拉框：选你的驱动板（一般是名字带 `Single_Serial` 的那个）。选不到就点 **刷新**。
- **连接**：连上舵机。右边 8 个小圆点 → **全变绿 = 8 个舵机都在线**。
- 变红或缺几个：检查排线、供电，点 **刷新** 再 **连接**。

> ⚠️ 控制台连着串口时会**独占端口**。此时别再用命令行去控制手，二选一。

---

## 三、四个标签页怎么用

### 1）设置 ID（新舵机才需要）
每个手指两个舵机，出厂ID都是 1，必须先改成约定的编号：

**食指 1/2 ｜ 中指 3/4 ｜ 无名指 5/6 ｜ 拇指 7/8**

铁律（很重要）：
- **一次只在总线上接【一个】要改的新舵机**，别一次接一堆——ID 撞车就扫不到。
- 先点 **扫描确认**，应当只显示 `[1]`。
- 填「当前ID → 新ID」，勾上 **解锁写入(推荐)**（不勾改完断电就丢），点 **设置 ID**。
- 改完再接下一个。全部改完，8 个都接上。

### 2）标定（装好新手指后做）
目的：找到每个舵机的「中位零点」，装摇臂时对齐；再微调让左右对称。
- 先点 **中位**：舵机回到中间位置，这时候再装摇臂/连杆。
- 点 **闭合**：看手指弯曲**方向对不对、两边对不对称**。
- 不对称就调 `off1 / off2`（补偿角度），再点 **中位/闭合** 看效果。
- 满意后点 **💾 保存标定** → 写进 `calibration.txt`，下次自动生效。
- **松开全部扭矩**：让舵机松掉，可以用手自由掰动。

### 3）玩一玩（现成动作）
点按钮就跑（会**先自动断开串口**让子程序独占，跑的时候别手动控制）：
- **🖐 整手花样 Demo**：整只手做一套花样动作。
- **✊✌✋ 剪刀石头布**：依次展示石头、剪刀、布。
- **🎲 随机猜拳 5 局**：手随机出拳，跟你对战。
- **⏹ 停止**：结束当前动作。

### 4）手势控制（摄像头 → 手实时跟随）
用摄像头识别你的手，让机械手（或仿真）跟着动。
- 第一次先点 **🔧 首次构建**（装依赖，慢一点，耐心等）。
- **▶ 启动仿真（不碰硬件）**：只在电脑里的 3D 手上跟随，安全，先用它试。
- **▶ 启动真手**：仿真 + 真机械手一起动（需要装 Rust）。
- **⏹ 停止手势控制**：结束。
- 摄像头打不开：多半是摄像头序号变了，改 `Demo/HandTracking/HandTracking/main.py` 里的 `VideoCapture(N)` 的数字（0、6 之类挨个试）。

---

## 四、常见问题速查

| 现象 | 原因 / 解决 |
|---|---|
| 串口下拉里没有设备 | 板子没插好 / 点 **刷新**；名字认带 `Single_Serial` 的 |
| 连接后圆点不全绿 | 排线松、供电不足、某个舵机ID没设对 |
| 设ID后断电就没了 | 没勾 **解锁写入**，重设一次并勾上 |
| 扫描能扫到 1 但设不了 | 总线上接了多个舵机，只留要改的那一个 |
| 剪刀/动作跑不了 | 控制台可能还占着串口——点 **停止** 或先 **断开** |
| 手势摄像头黑屏 | 改 `main.py` 里 `VideoCapture(N)` 的序号 |
| 报没有 tkinter | 运行 `sudo apt-get install -y python3-tk` |
| 加了 dialout 组还连不上 | 需要**注销重新登录**一次才生效 |

---

## 五、里面有什么

```
AmazingHand-小白包/
├─ 打开控制台.sh          ← 双击打开图形界面
├─ 安装.sh                ← 第一次装环境用
├─ 使用说明_控制台UI.md   ← 本文件
├─ PythonExample/         ← 控制/标定/Demo/猜拳 的程序
│   ├─ AmazingHand_GUI.py       图形控制台（主程序）
│   ├─ AmazingHand_Calib.py     零依赖底层控制/标定
│   ├─ AmazingHand_SetID.py     设置舵机 ID
│   ├─ AmazingHand_RPS.py       剪刀石头布
│   ├─ AmazingHand_Demo_NoDeps.py  花样 Demo
│   └─ calibration.txt          标定记录
├─ Demo/                  ← 手势控制（摄像头跟随）
└─ docs/                  ← 中文手册 PDF（复刻手册、映射原理等）
```

更详细的原理和踩坑，看 `docs/` 里的中文手册 PDF。

祝玩得开心 🖐
